#include <iostream>
using namespace std;

int main()
{
    cout<<"hello everyone "<<endl<<"this is a fibonacci series";
    cout<<"hence enter the value above 2"<<endl<<"as formula of fibonaci series is mention below:";

    cout<<endl<<"\"F(n)=f(n-1)+F(n-2)\"";
    cout<<"function F=(n+1)/n";
    cout<<"n must be greater than 2 for validation of the formula";
   
    int n;
    cin >>n;
    int i=(n-1);
    int p=(n-2)
    int t=0;
    int f=0;
    if(n>2 && i==(n-1))
    {
        f=(i+1)/i;
        int x=return (f);}
    cout<<"F(n-1), value is :"<<x;
    if(n>2 && p==(n-2))
    {
        t=(p+1)/p;
        int n= return (t);}
    
    cout<<"F(n-2),value is:"<<n;
    cout<<"F,value is :"<<x+n;

    




}